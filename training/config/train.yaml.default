---
experiment_desc: basic_model

train:
    dataset: MNIST
    dataset_path: files/train.csv
val:
    dataset: MNIST
    dataset_path: files/train.csv

model:
    name: lstm
    embedding_dim: 300
    num_features:
        - 256
        - 256
    num_classes: 3

    loss:
        name: cross_entropy

optimizer:
    name: adam
    lr: 0.0001

batch_size: 1
num_epochs: 50

steps_per_epoch: 2000
validation_steps: 200
